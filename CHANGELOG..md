# บันทึกการเปลี่ยนแปลง (CHANGELOG.md)

การเปลี่ยนแปลงที่สำคัญทั้งหมดของโครงการ Wordle CLI ถูกบันทึกไว้ในไฟล์นี้ โดยปฏิบัติตามรอบการพัฒนา Kanban WIP=1 ของโครงการ

## [v1.0.0] - 2026-07-27 — เปิดตัวอย่างเป็นทางการ
### สิ่งที่เพิ่มเข้ามา
- เปิดตัว Wordle CLI เวอร์ชันแรกอย่างเป็นทางการ ครอบคลุมผลการเรียนรู้ (Learning Outcomes) ทั้ง 5 ข้อ
- ตรวจสอบกรณีขอบเขต (Edge-case validation) สมบูรณ์แบบ เช่น ความยาวข้อความ, ข้อมูลที่ไม่ใช่ตัวอักษร และการเลือกเมนูที่ไม่ถูกต้อง
- ตรวจสอบโค้ดขั้นสุดท้ายตามเกณฑ์การประเมินระดับ "ดีเยี่ยม (4)" ในเกณฑ์การให้คะแนน

## [v0.7.0] - 2026-07-27
### สิ่งที่เพิ่มเข้ามา
- เพิ่มตัวเลือกที่ 3: ลบคำออกจาก Active Pool โดยใช้ `.remove()` (LO2)
- เพิ่มการตรวจสอบสมาชิกเชิงรุก `if target_word in word_pool:` เพื่อขจัดข้อผิดพลาด `ValueError` ขณะรันไทม์เมื่อลบคำที่ไม่มีอยู่ (LO4)
- เพิ่มระบบป้องกันคลังคำว่างในตัวเลือกที่ 1 และ 3 ให้รีเซ็ต `word_pool` จาก `DEFAULT_WORDS` อัตโนมัติเมื่อความยาวลดลงเหลือ 0

## [v0.6.0] - 2026-07-27
### สิ่งที่เพิ่มเข้ามา
- เพิ่มตัวเลือกที่ 2: ดูและเรียงลำดับประวัติการทายคำ (LO2)
- ใช้ `len(guess_history)` เพื่อรายงานจำนวนการทายทั้งหมดที่บันทึกไว้
- เพิ่มการเรียงลำดับลิสต์ในตำแหน่งเดิมด้วย `guess_history.sort()`
- เพิ่มการตรวจสอบป้องกัน `if len(guess_history) == 0:` เพื่อแสดงข้อความที่ชัดเจนเมื่อประวัติยังว่างอยู่ (LO4)

## [v0.5.0] - 2026-07-27
### สิ่งที่เพิ่มเข้ามา
- เพิ่มการตรวจสอบความถูกต้องเชิงป้องกันด้วย `len()` และ `.isalpha()` ภายในลูปทายคำของตัวเลือกที่ 1 (LO4)
- ป้องกันข้อมูลที่ไม่ถูกต้อง (ตัวเลข, สัญลักษณ์, คำที่มีความยาวผิด) ไม่ให้เกิดข้อผิดพลาด `IndexError` ระหว่างประเมินผล
- ปกป้องตัวนับ `attempts` และลิสต์ `guess_history` เพื่อให้ข้ามข้อมูลที่ไม่ถูกต้องโดยไม่เสียสิทธิ์การทาย โดยใช้ `continue`

## [v0.4.0] - 2026-07-27
### สิ่งที่เพิ่มเข้ามา
- แปลง Tuple `DEFAULT_WORDS` ที่แก้ไขไม่ได้ให้เป็นลิสต์ `word_pool` แบบไดนามิกโดยใช้ `list()` (LO1)
- สร้างลูปการทายคำหลักสำหรับตัวเลือกที่ 1 โดยใช้ `random.choice()`
- ใช้ `.append()` เพื่อบันทึกทุกคำทายลงใน `guess_history` โดยตรง (LO2)
- สร้างตรรกะประเมินตำแหน่งตัวอักษรเพื่อสร้างสัญลักษณ์ตอบกลับ (`✓`, `-`, `x`)

## [v0.3.0] - 2026-07-27
### สิ่งที่เพิ่มเข้ามา
- สร้างลูปควบคุมโต้ตอบแบบไม่รู้จบด้วย `while True` (LO3)
- ใช้โครงสร้างเงื่อนไขหลายทาง (`if/elif/else`) สำหรับการนำทางเมนู
- เพิ่มการปรับมาตรฐานข้อมูลเข้าด้วย `.strip()` และตัวเลือกการออกจากโปรแกรมอย่างนุ่มนวล (`break`)
- เพิ่มข้อความตอบกลับข้อผิดพลาดสำหรับการเลือกเมนูที่ไม่ถูกต้องเพื่อป้องกันสถานะที่ไม่ได้รับการจัดการ

## [v0.2.0] - 2026-07-27
### สิ่งที่เพิ่มเข้ามา
- กำหนด Tuple คอนฟิกเกมที่แก้ไขไม่ได้ `GAME_CONFIG` และ `DEFAULT_WORDS` ใน `main.py` (LO1)
- เพิ่มตรรกะ Tuple unpacking ใน `main()` เพื่อตรวจสอบว่าการอ่านค่าคอนฟิกทำงานได้อย่างถูกต้อง

## [v0.1.0] - 2026-07-27
### สิ่งที่เพิ่มเข้ามา
- สร้างโครงสร้างคลังจัดเก็บโค้ดเริ่มต้น (`main.py`, `CHANGELOG.md`, `learning_log.ipynb`)
- จัดตั้งกระบวนการทำงานแบบ Kanban โดยจำกัด WIP ไว้อย่างเข้มงวดที่ 1
