# บันทึกการเรียนรู้ — Wordle CLI

**โปรเจกต์:** Wordle CLI\
**กระบวนการทำงาน:** Kanban, จำกัด WIP = 1, ทำทีละการ์ดต่อรอบ

บันทึกนี้เก็บข้อมูลคำสั่ง AI ทุกคำสั่งที่ใช้, การตัดสินใจทางเทคนิค และอุปสรรคในการแก้ไขบั๊กที่พบตลอดทั้ง 8 รอบของการพัฒนา

---

## บันทึกรอบที่ 1: การตั้งค่าโปรเจกต์และกระบวนการบันทึก
**เป้าหมายผลการเรียนรู้:** LO5 (การทำเอกสารกระบวนการและบันทึกข้อมูล)
**คำสั่ง AI ที่ใช้:** "execute the kanban backlog, wip limit=1, run in a cycle, one task at a time, update kanban backlog, changelog, learning_log after a cycle done."

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**
- การตั้งค่าระบบบันทึกและเอกสารเวอร์ชัน *ก่อน* การเขียนตรรกะหลัก ช่วยป้องกันการมาไล่บันทึกย้อนหลัง
- การจัดโครงสร้าง `CHANGELOG.md` ด้วย Semantic Versioning (`v0.1.0`) ช่วยสร้างประวัติการวิวัฒนาการของโค้ดที่ตรวจสอบได้

**การแก้บั๊กและอุปสรรค:** ไม่พบอุปสรรคในช่วงเริ่มต้นโปรเจกต์

---

## บันทึกรอบที่ 2: การออกแบบคอนฟิกแบบแก้ไขไม่ได้
**เป้าหมายผลการเรียนรู้:** LO1 (การเลือกโครงสร้างข้อมูล — Tuples vs. Lists)
**คำสั่ง AI ที่ใช้:** "run the next cycle"

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**
- `GAME_CONFIG` และ `DEFAULT_WORDS` ถูกกำหนดให้เป็น Tuple เพราะกฎของเกม (5 ตัวอักษร, ทายได้ 6 ครั้ง) และคลังคำสำรองไม่ควรถูกเปลี่ยนแปลงขณะโปรแกรมทำงาน
- หากใช้ List แทน อาจเกิดเหตุการณ์ที่ไม่ตั้งใจเช่น `.pop()`, `.append()` หรือการกำหนดค่าใหม่ ซึ่งจะทำให้คอนฟิกหลักเสียหาย การใช้ Tuple จึงช่วยบังคับความปลอดภัยทางสถาปัตยกรรมโดยจะแจ้งเตือน `TypeError` ทันทีหากมีการพยายามแก้ไข

**การแก้บั๊กและอุปสรรค:** ตรวจสอบแล้วว่าการกระจายค่า Tuple `word_length, max_attempts = GAME_CONFIG` ทำงานได้ถูกต้องโดยไม่เกิดข้อผิดพลาดด้านดรรชนี (Indexing)

---

## บันทึกรอบที่ 3: สถาปัตยกรรมเมนูและทิศทางการทำงาน
**เป้าหมายผลการเรียนรู้:** LO3 (การควบคุมทิศทางการทำงานแบบตอบโต้)
**คำสั่ง AI ที่ใช้:** "run the next cycle"

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**
- ใช้ `while True` ร่วมกับคำสั่ง `break` ที่ชัดเจนในตัวเลือกที่ 4 เพื่อให้เมนูถามซ้ำเรื่อยๆ จนกว่าผู้ใช้จะสั่งออกจากโปรแกรม
- ใส่ `.strip()` ในฟังก์ชัน `input()` เพื่อไม่ให้ช่องว่างส่วนเกินส่งผลให้การเลือกตัวเลขถูกมองว่าผิดพลาด
- จัดการกรณีผู้ใช้กรอกตัวเลือกที่ไม่คาดคิด (`"9"`, `"abc"`, `""`) ในส่วน `else` เพื่อให้โปรแกรมยังคงทำงานต่อได้โดยไม่เกิดข้อผิดพลาดรุนแรง

**การแก้บั๊กและอุปสรรค:** ทดสอบป้อนค่าที่ไม่ใช่ตัวเลขและค่าว่าง ยืนยันว่าโปรแกรมแสดงข้อความแจ้งเตือนและถามใหม่ได้ถูกต้องโดยไม่ค้าง

---

## บันทึกรอบที่ 4: เซตข้อมูลไดนามิกและตรรกะการประเมิน
**เป้าหมายผลการเรียนรู้:** LO1 (Mutable Collections) & LO2 (`.append()`)
**คำสั่ง AI ที่ใช้:** "run the next cycle"

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**
- สร้าง `word_pool = list(DEFAULT_WORDS)` เพื่อให้โปรแกรมมีสำเนาคลังคำที่แก้ไขได้ โดยไม่ไปทำลาย Tuple สำรองเดิม
- ใช้ `guess_history.append(user_input)` ในลูปการทายคำเพื่อเก็บประวัติของผู้เล่น
- สร้าง List ชั่วคราว `feedback = []` เพื่อรวบรวมสัญลักษณ์ (`✓`, `-`, `x`) ก่อนจะรวมเข้าด้วยกันเป็นข้อความแสดงผล

**การแก้บั๊กและอุปสรรค:** การกรอกคำที่มี 3 ตัวอักษร (เช่น "CAT") ทำให้เกิด `IndexError` ระหว่างการประเมินตำแหน่ง เนื่องจาก `user_input[i]` พยายามเข้าถึงดรรชนีที่ไปถึงตำแหน่งที่ 4 จึงได้ทำเครื่องหมายไว้เพื่อแก้ไขในรอบถัดไป

---

## บันทึกรอบที่ 5: การป้องกันข้อมูลป้อนเข้าไม่ถูกต้อง
**เป้าหมายผลการเรียนรู้:** LO4 (การเขียนโปรแกรมเชิงป้องกัน)
**คำสั่ง AI ที่ใช้:** "run the next cycle"

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**
- ย้ายคำสั่ง `.append()` และ `attempts += 1` ไปไว้ใต้จุดตรวจสอบข้อมูลเข้า หากตรวจสอบไม่ผ่าน คำสั่ง `continue` จะข้ามลูปส่วนที่เหลือ ทำให้สถานะเกมยังคงถูกต้อง
- การบังคับใช้ `len(user_input) != word_length` ก่อนการเข้าถึงดรรชนี ช่วยกำจัดข้อผิดพลาด `IndexError` ที่พบในรอบที่ 4 ได้อย่างสมบูรณ์

**การแก้บั๊กและอุปสรรค:** ทดสอบคำว่า `"CAT"` (สั้นไป), `"ELEPHANTS"` (ยาวไป), `"12345"` (ตัวเลข) ทั้งหมดแสดงข้อความแจ้งเตือนและให้พิมพ์ใหม่โดยไม่เสียสิทธิ์การทายและไม่ค้าง

---

## บันทึกรอบที่ 6: การตรวจสอบประวัติและการเรียงลำดับ
**เป้าหมายผลการเรียนรู้:** LO2 (`len`, `.sort`) & LO4 (การตรวจสอบเชิงป้องกัน)
**คำสั่ง AI ที่ใช้:** "run the next cycle"

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**
- ใช้ `if len(guess_history) == 0:` ก่อนจัดรูปแบบ/เรียงลำดับ เพื่อให้ตัวเลือกที่ 2 แสดงผลอย่างสะอาดเมื่อเปิดโปรแกรมใหม่
- `guess_history.sort()` จะแก้ไขและเรียงลำดับข้อมูลใน List เดิม ต่างจาก `sorted()` ที่จะคืนค่าเป็น List ใหม่

**การแก้บั๊กและอุปสรรค:** กดเลือกตัวเลือกที่ 2 ทันทีหลังเปิดโปรแกรม ยืนยันว่าแสดงข้อความ "ยังไม่มีประวัติการทาย" แทนที่จะแสดงวงเล็บว่างเปล่าหรือข้อผิดพลาด

---

## บันทึกรอบที่ 7: การลบ List อย่างปลอดภัยและการตรวจสอบสมาชิก
**เป้าหมายผลการเรียนรู้:** LO2 (`.remove()`) & LO4 (การเขียนโปรแกรมเชิงป้องกัน)
**คำสั่ง AI ที่ใช้:** "run the next cycle"

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**
- การใช้ `.remove()` กับข้อมูลไม่มีอยู่จะเกิด `ValueError` การป้องกันด้วย `if target_word in word_pool:` ช่วยจัดการปัญหานี้ได้อย่างราบรื่น
- เมื่อรวมกับการตรวจสอบ `len(word_pool) == 0` โปรแกรมจะฟื้นตัวกลับมาโดยการโหลด `DEFAULT_WORDS` ใหม่โดยอัตโนมัติหากผู้ใช้ลบคำจนหมด

**การแก้บั๊กและอุปสรรค:** พยายามลบคำว่า `"BANANA"` (ซึ่งไม่อยู่ในคลัง) — ตัวป้องกันดักจับไว้ได้ และแสดงข้อความแจ้งเตือนโดยไม่ทำให้ลูปเมนูหลักพัง

---

## บันทึกรอบที่ 8: การรีแฟกเตอร์ขั้นสุดท้ายและการสรุปภาพรวม
**เป้าหมายผลการเรียนรู้:** LO5 & เกณฑ์ประเมิน: ความเป็นเจ้าของกระบวนการแบบสะท้อนคิด

**การประเมินตนเองตามผลการเรียนรู้:**
1. **LO1** — ประยุกต์ใช้ Tuples สำหรับกฎที่แก้ไขไม่ได้ และใช้ Lists สำหรับสถานะเกมที่เปลี่ยนแปลงได้
2. **LO2** — ใช้งาน `.append()`, `.remove()`, `.sort()` และ `len()` ได้อย่างถูกต้องทั่วทั้งโปรแกรม
3. **LO3** — สร้างลูป `while True` ที่ทำงานต่อเนื่อง พร้อมโครงสร้าง `if/elif/else` หลายทางและการออกจากโปรแกรมที่สะอาด (`break`)
4. **LO4** — ป้องกันการแก้ไข List ทุกจุด (ตรวจ `in` ก่อน `.remove()`) และการจัดการข้อความ (`len()`, `.isalpha()`) ขจัดปัญหารันไทม์ค้าง
5. **LO5** — บันทึกกระบวนการพัฒนาครบทั้ง 8 รอบ พร้อมประวัติคำสั่ง AI, ผลต่างของโค้ด และโน้ตการแก้บั๊ก

**การตรวจสอบความสอดคล้องกับเกณฑ์การประเมิน:**
- ความถูกต้องและความเข้มงวดทางเทคนิค: 4/4 — การเลือกใช้โครงสร้างข้อมูลตรงตามความต้องการของสถานะ; ไม่มีข้อผิดพลาดที่ไม่ได้รับการจัดการ
- การใช้งานสถาปัตยกรรม: 4/4 — การนำทางเมนูสะอาด ออกจากโปรแกรมได้ราบรื่น ปรับมาตรฐานข้อมูลเข้าเรียบร้อย
- ความเป็นเจ้าของกระบวนการแบบสะท้อนคิด: 4/4 — มีประวัติการทำงานสมบูรณ์ทั้งในบันทึกนี้, `CHANGELOG.md` และกระดาน Kanban


## บันทึกรอบที่ 9: การรีแฟกเตอร์สถาปัตยกรรมโค้ดและระบบสถิติ
**เป้าหมายผลการเรียนรู้:** LO1 (Dictionary Data Structure), LO5 (Documentation & Refactoring)
**คำสั่ง AI ที่ใช้:** "refactor code into modular functions, change guess_history to store dictionaries for statistics, add player stats and how to play menus, remove word removal feature."

**การตัดสินใจทางเทคนิคและการสะท้อนคิด:**
- **Modularization:** โค้ดเดิมยาวและอ่านยาก จึงตัดสินใจแยกตรรกะออกเป็นฟังก์ชันย่อย เช่น `validate_input` และ `evaluate_guess` ทำให้โค้ดสะอาดขึ้น ทดสอบง่ายขึ้น และมี Docstrings อธิบายชัดเจน (LO5)
- **Dictionaries:** การใช้ List ของ String แบบเดิมไม่สามารถตอบโจทย์การทำสถิติได้ จึงเปลี่ยน `guess_history` ให้เก็บ Dictionary แทน (`{"secret_word": ..., "won": ..., "guesses": ...}`) ซึ่งแสดงให้เห็นถึงความเข้าใจในการใช้ Data Structures ที่ซับซ้อนขึ้น (LO1)
- **List Comprehensions:** ในส่วนของ `display_statistics` นำ List Comprehension และ `sum()` มาใช้ประมวลผลข้อมูลสถิติ ซึ่งมีประสิทธิภาพมากกว่าลูปแบบดั้งเดิม (LO2)

**การแก้บั๊กและอุปสรรค:** 
- ในตอนแรกพบความเสี่ยงที่จะเกิด `ZeroDivisionError` หากผู้เล่นกดดูสถิติ (เมนู 3) ก่อนเล่นเกม จึงได้นำเทคนิค Defensive Programming (LO4) มาใช้ตรวจสอบ `if not guess_history:` เพื่อดักทางไว้ก่อน
